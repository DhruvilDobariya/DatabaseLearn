Select IfNull("Dhruvil", "Name is Not Available");
Select IfNull(NULL, "Name is Not Available");

-- Id expression1 is not null then return it otherwise it will return expression2.
-- IfNull(expression1, expression2); 